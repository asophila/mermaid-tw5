---
phase: 05-developer-experience
plan: 02
type: execute
wave: 1
depends_on:
  - 05-01-PLAN.md
files_modified:
  - README.md
  - CONTRIBUTING.md
autonomous: true
requirements:
  - DOCS-02

must_haves:
  truths:
    - "README.md contains a 'Development' section with setup, test, and build instructions (D-13)"
    - "README.md contains a 'Contributing' link pointing to CONTRIBUTING.md (D-20)"
    - "CONTRIBUTING.md exists at project root with coding standards, PR checklist, and branch naming (D-14, D-15, D-16, D-17, D-18)"
    - "Coding standards reference CONVENTIONS.md: 4-space indent, var/let consistency, single quotes, semicolons, no console.log in production (D-15)"
    - "PR checklist includes: tests pass, no new console.log, consistent style, describe what changed (D-16)"
  artifacts:
    - path: "README.md"
      provides: "User-facing readme with development quick-start"
      contains: "## Development"
    - path: "CONTRIBUTING.md"
      provides: "Full contribution guidelines"
      contains: "## Coding Standards"
  key_links: []
---

<objective>
Add developer setup instructions to README.md and create CONTRIBUTING.md with coding standards, PR process, and contribution guidelines.

Purpose: Lower the barrier for new contributors and establish clear expectations for code quality and PR hygiene.
Output: Updated README.md with Development section and new CONTRIBUTING.md at project root.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-developer-experience/05-CONTEXT.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STACK.md
@.planning/codebase/STRUCTURE.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Development section to README.md</name>
  <files>
    README.md
  </files>
  <read_first>
    README.md
    .planning/phases/05-developer-experience/05-CONTEXT.md
    .planning/codebase/CONVENTIONS.md
  </read_first>
  <action>Append a "Development" section to `README.md` before the "Notes" section. The new section should be inserted just before the line that starts with "Notes:" or before the first numbered note.

Add the following markdown:

```markdown
## Development

### Running Tests

This project uses Node.js built-in test runner. No npm install is required.

```bash
node --test
```

Tests live in the `tests/` directory and cover widget rendering, typed parser behavior, and utility functions.

### Building the Demo

The demo site is a pre-built TiddlyWiki hosted on GitHub Pages. To rebuild it locally:

```bash
npm install -g tiddlywiki
tiddlywiki mermaid-tw5 --build index
cp mermaid-tw5/output/index.html docs/index.html
```

### Contributing

See [CONTRIBUTING.md](CONTRIBUTING.md) for coding standards and the pull request process.
```

Preserve all existing README content (credits, installation instructions, notes about bundle size).
</action>
  <acceptance_criteria>
    - `README.md` contains `## Development`
    - `README.md` contains `node --test` in a code block
    - `README.md` contains `npm install -g tiddlywiki` in a code block
    - `README.md` contains a link to `CONTRIBUTING.md`
    - All original README content is preserved
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create CONTRIBUTING.md</name>
  <files>
    CONTRIBUTING.md
  </files>
  <read_first>
    .planning/phases/05-developer-experience/05-CONTEXT.md
    .planning/codebase/CONVENTIONS.md
    .planning/codebase/STACK.md
  </read_first>
  <action>Create `CONTRIBUTING.md` at the project root with the following content:

```markdown
# Contributing to mermaid-tw5

Thank you for your interest in improving the mermaid-tw5 plugin!

## Getting Started

1. Fork the repository
2. Clone your fork
3. Run tests to ensure everything is working:
   ```bash
   node --test
   ```

## Coding Standards

We follow the conventions documented in the codebase. Key points:

- **Indentation:** 4 spaces (no tabs)
- **Quotes:** Single quotes for strings
- **Semicolons:** Use them consistently
- **Variable declarations:** Prefer `let` for new code; `var` is acceptable for consistency within existing functions
- **No debug logging:** Do not leave `console.log` or `console.error` in production code
- **Strict mode:** All modules begin with `'use strict';`

## Pull Request Process

1. Create a branch from `main` with a descriptive name:
   - `feature/description` for new capabilities
   - `fix/description` for bug fixes
2. Make your changes
3. Run tests locally: `node --test`
4. Ensure your code follows the coding standards above
5. Open a pull request to `main` with:
   - A clear description of what changed and why
   - Confirmation that tests pass
   - Note if the change affects the demo site (may need a rebuild)

## What to Expect

- Automated tests will run on your PR via GitHub Actions
- The demo site is rebuilt and deployed automatically when PRs are merged to `main`
- Reviews focus on correctness, consistency with existing patterns, and test coverage

## Reporting Issues

When reporting bugs, please include:
- TiddlyWiki version
- Browser and version (if applicable)
- Steps to reproduce
- Sample Mermaid syntax that triggers the issue
- Any error messages (text or screenshot)
```
</action>
  <acceptance_criteria>
    - `CONTRIBUTING.md` exists at project root
    - `CONTRIBUTING.md` contains `## Coding Standards`
    - `CONTRIBUTING.md` contains `node --test`
    - `CONTRIBUTING.md` contains `feature/description` and `fix/description` branch naming examples
    - `CONTRIBUTING.md` contains a PR checklist or process description
    - `CONTRIBUTING.md` contains issue reporting guidelines
  </acceptance_criteria>
</task>

</tasks>

<verification>
- README.md has a Development section with setup, test, and build instructions
- README.md links to CONTRIBUTING.md
- CONTRIBUTING.md exists with coding standards, PR process, and issue reporting
- Both files are valid markdown
</verification>
