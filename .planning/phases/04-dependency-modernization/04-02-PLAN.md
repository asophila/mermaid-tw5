---
phase: 04-dependency-modernization
plan: 02
type: execute
wave: 1
depends_on:
  - 04-01
files_modified:
  - .planning/PROJECT.md
autonomous: true
requirements:
  - UPDT-01

must_haves:
  truths:
    - "Adoption criteria from CONTEXT.md (D-14) are evaluated against measured data: bundle ≤1.2 MB, all diagrams render, API migration straightforward, D3 zoom works"
    - "Deferral criteria from CONTEXT.md (D-15) are triggered: all modern versions exceed 1.5 MB threshold"
    - "Deferral decision is documented in PROJECT.md Key Decisions table with rationale and upstream tracking reference"
    - "Re-evaluation trigger is specified: revisit when upstream lite build is available or bundle drops below 1.2 MB"
    - "ROADMAP.md Phase 4 status reflects completion of evaluation (success criteria 1, 2, 3, 5 met)"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Updated Key Decisions table with deferral rationale"
      contains: "Defer Mermaid upgrade"
  key_links: []
---

<objective>
Document the deferral decision for modern Mermaid.js adoption in `.planning/PROJECT.md` Key Decisions table. Update the decision entry for "Stay on Mermaid 9.3.0" from ⚠️ Revisit to a firm deferral with measured rationale, upstream tracking reference, and a re-evaluation trigger.

Purpose: Close Phase 4 with a documented, evidence-based decision that satisfies UPDT-01 and all ROADMAP success criteria.
Output: Updated PROJECT.md with deferral decision, and updated ROADMAP.md/STATE.md reflecting Phase 4 completion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-dependency-modernization/04-CONTEXT.md
@.planning/phases/04-dependency-modernization/04-RESEARCH.md
@.planning/phases/04-dependency-modernization/04-01-PLAN.md
@.planning/phases/04-dependency-modernization/04-SIZE-COMPARISON.md (produced by Plan 01)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update PROJECT.md Key Decisions with deferral rationale</name>
  <files>.planning/PROJECT.md</files>
  <read_first>.planning/PROJECT.md</read_first>
  <action>Update the `.planning/PROJECT.md` Key Decisions table. Find the existing row:

```markdown
| Stay on Mermaid 9.3.0 | Mermaid 10.x is 3.2 MB minified, considered too large | ⚠️ Revisit — missing security patches and new diagram types |
```

Replace it with:

```markdown
| Stay on Mermaid 9.3.0 | Measured v10.9.0=3.34 MB, v11.14.0=3.16 MB, tiny=2.12 MB; all exceed 1.5 MB deferral threshold. No lite build available upstream (#4616). Promise-based API migration is feasible (~15–25 lines) but size alone blocks adoption. | ⚠️ Deferred — Revisit when upstream lite build <1.2 MB or Mermaid provides modular UMD bundles. Tracked: mermaid-js/mermaid#4616 |
```

Also update the "Lazy loading of mermaid.js and D3.js" decision entry from:
```markdown
| Lazy loading of mermaid.js and D3.js | ... | ✓ Good — reduces initial load for non-diagram pages |
```

To add a note about Phase 4's impact on the size constraint:
```markdown
| Lazy loading of mermaid.js and D3.js | Defer library loading until first diagram render; pages without diagrams save ~1.17 MB. Phase 4 confirmed modern Mermaid is 2.1–3.3 MB, making lazy loading even more critical for controlling perceived load. | ✓ Good — reduces initial load for non-diagram pages |
```

Ensure the table formatting (pipe alignment) is preserved.
</action>
  <verify>grep -c "Deferred" .planning/PROJECT.md && grep -c "mermaid-js/mermaid#4616" .planning/PROJECT.md</verify>
  <acceptance_criteria>
    - `.planning/PROJECT.md` contains the word "Deferred" in the Key Decisions table
    - `.planning/PROJECT.md` contains "mermaid-js/mermaid#4616"
    - `.planning/PROJECT.md` contains "v11.14.0=3.16 MB" or similar size reference
    - `.planning/PROJECT.md` contains "1.5 MB deferral threshold"
    - The Key Decisions table still has valid markdown formatting (pipes and alignment)
    - The "Stay on Mermaid 9.3.0" row is updated (not duplicated)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md to mark UPDT-01 as evaluated</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>.planning/REQUIREMENTS.md</read_first>
  <action>Update `.planning/REQUIREMENTS.md` to reflect that UPDT-01 has been evaluated and deferred:

1. In the v1 Requirements section, change:
```markdown
- [ ] **UPDT-01**: Evaluate modern Mermaid.js versions (or lite builds) and adopt if bundle size is acceptable
```

To:
```markdown
- [x] **UPDT-01**: Evaluate modern Mermaid.js versions (or lite builds) and adopt if bundle size is acceptable — Evaluated in Phase 4; deferred. All modern versions (10.9.0, 11.14.0, tiny) exceed size threshold.
```

2. In the Traceability table, update the UPDT-01 row from:
```markdown
| UPDT-01 | Phase 4 | Pending |
```

To:
```markdown
| UPDT-01 | Phase 4 | Deferred |
```
</action>
  <verify>grep "UPDT-01" .planning/REQUIREMENTS.md | head -3</verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` shows UPDT-01 as checked (`[x]`)
    - The UPDT-01 line contains "Evaluated in Phase 4" or "deferred"
    - The Traceability table shows UPDT-01 status as "Deferred"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Update STATE.md to reflect Phase 4 completion</name>
  <files>.planning/STATE.md</files>
  <read_first>.planning/STATE.md</read_first>
  <action>Update `.planning/STATE.md` to mark Phase 4 as complete:

1. Update the `progress` block:
```yaml
progress:
  total_phases: 5
  completed_phases: 4
  total_plans: 6
  completed_plans: 6
  percent: 80
```

2. Update the Phase Tracker table:
```markdown
| Phase | Status | Requirements | Plans | Progress |
|-------|--------|--------------|-------|----------|
| 1 | ✓ | 3/3 | 2/2 | 100% |
| 2 | ✓ | 2/2 | 2/2 | 100% |
| 3 | ✓ | 1/1 | 2/2 | 100% |
| 4 | ✓ | 1/1 | 2/2 | 100% |
| 5 | ○ | 2/2 | 0/0 | 0% |
```

3. Update the status line:
```markdown
**Status:** Phase 4 complete — Mermaid upgrade deferred pending upstream lite build
```

4. Add to Recent Activity:
```markdown
- 2026-04-27: Phase 4 planned — dependency modernization evaluation
- 2026-04-27: Phase 4 executed — evaluated Mermaid 10.9.0, 11.14.0, and @mermaid-js/tiny; all exceed size threshold; deferral documented in PROJECT.md
```
</action>
  <verify>grep "Phase 4 | ✓" .planning/STATE.md && grep "percent: 80" .planning/STATE.md</verify>
  <acceptance_criteria>
    - `.planning/STATE.md` shows `completed_phases: 4`
    - `.planning/STATE.md` shows `percent: 80`
    - `.planning/STATE.md` shows Phase 4 status as `✓`
    - `.planning/STATE.md` shows Phase 4 Requirements as `1/1`
    - `.planning/STATE.md` shows Phase 4 Plans as `2/2`
    - `.planning/STATE.md` contains a recent activity entry for Phase 4 execution
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Update ROADMAP.md Phase 4 success criteria status</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>.planning/ROADMAP.md</read_first>
  <action>Update `.planning/ROADMAP.md` to reflect that Phase 4 success criteria have been met. In the Phase 4 section, after the success criteria list, add a status annotation:

Find:
```markdown
**Success Criteria:**
1. Modern Mermaid.js versions (10.x+) are evaluated for size and compatibility
2. Lite build or modular import strategy is investigated
3. Decision is documented in PROJECT.md Key Decisions
4. If adopted, all existing diagram examples render correctly
5. If deferred, rationale is documented with upstream tracking reference
```

Replace with:
```markdown
**Success Criteria:**
1. ✓ Modern Mermaid.js versions (10.x+) are evaluated for size and compatibility
2. ✓ Lite build or modular import strategy is investigated
3. ✓ Decision is documented in PROJECT.md Key Decisions
4. N/A — adoption criteria not met (size >1.5 MB)
5. ✓ Rationale documented with upstream tracking reference (mermaid-js/mermaid#4616)
```

Also update the Overview table to show Phase 4 as Complete:

Find:
```markdown
| 4 | Dependency Modernization | Evaluate and adopt a modern Mermaid.js version while controlling bundle size | UPDT-01 | 2 |
```

Replace with:
```markdown
| 4 | Dependency Modernization | Evaluate and adopt a modern Mermaid.js version while controlling bundle size | UPDT-01 | Complete | 2026-04-27 |
```

Ensure the table column alignment is preserved.
</action>
  <verify>grep "mermaid-js/mermaid#4616" .planning/ROADMAP.md && grep "Phase 4 | Dependency Modernization" .planning/ROADMAP.md</verify>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` contains "✓ Modern Mermaid.js versions" in Phase 4 success criteria
    - `.planning/ROADMAP.md` contains "N/A — adoption criteria not met" in Phase 4 success criteria
    - `.planning/ROADMAP.md` contains "mermaid-js/mermaid#4616" in Phase 4 success criteria
    - `.planning/ROADMAP.md` Overview table shows Phase 4 as "Complete"
    - The Overview table date for Phase 4 is "2026-04-27"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Run existing test suite to ensure no regressions</name>
  <files>
    tests/wrapper.test.js
    tests/typed-parser.test.js
    tests/widget-tools.test.js
  </files>
  <read_first>tests/wrapper.test.js</read_first>
  <action>Run the full test suite to confirm documentation-only changes have not broken any existing functionality:

```bash
node --test
```

This plan modifies only `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/STATE.md`, and `.planning/ROADMAP.md`. No plugin source files are touched.
</action>
  <verify>node --test</verify>
  <acceptance_criteria>
    - Command `node --test` exits with code 0
    - All 13 tests continue to pass
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `.planning/PROJECT.md` Key Decisions table contains deferral decision with size rationale
- [ ] `.planning/PROJECT.md` contains upstream tracking reference `mermaid-js/mermaid#4616`
- [ ] `.planning/REQUIREMENTS.md` marks UPDT-01 as `[x]` with "Evaluated in Phase 4; deferred"
- [ ] `.planning/REQUIREMENTS.md` Traceability table shows UPDT-01 status as "Deferred"
- [ ] `.planning/STATE.md` shows `completed_phases: 4` and `percent: 80`
- [ ] `.planning/STATE.md` shows Phase 4 as `✓` with `1/1` requirements and `2/2` plans
- [ ] `.planning/ROADMAP.md` Overview table shows Phase 4 as "Complete"
- [ ] `.planning/ROADMAP.md` Phase 4 success criteria show 4 of 5 as met (criterion 4 N/A)
- [ ] `node --test` exits with code 0
</verification>

<success_criteria>
- PROJECT.md Key Decisions documents the deferral with measured size data
- PROJECT.md includes upstream tracking reference (mermaid-js/mermaid#4616)
- PROJECT.md includes re-evaluation trigger (lite build <1.2 MB or modular UMD)
- REQUIREMENTS.md marks UPDT-01 as evaluated/deferred
- STATE.md reflects Phase 4 completion (4/5 phases, 80%)
- ROADMAP.md reflects Phase 4 completion in Overview and success criteria
- All existing tests pass without regression
</success_criteria>

<output>
After completion, create `.planning/phases/04-dependency-modernization/04-02-SUMMARY.md`
</output>
