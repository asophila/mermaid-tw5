---
phase: 01-cleanup-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mermaid-tw5/plugins/mermaid-tw5/$__plugins_mermaid-tw5_wrapper.js
  - mermaid-tw5/plugins/mermaid-tw5/$__plugins_mermaid-tw5_widget-tools.js
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-02

must_haves:
  truths:
    - "wrapper.js contains no console.log statements"
    - "widget-tools.js contains no console.error(ex) in getOptions"
    - "wrapper.js contains no commented-out Node.js/browser detection block"
    - "wrapper.js contains no commented-out window.mermaidAPI.render call"
  artifacts:
    - path: "mermaid-tw5/plugins/mermaid-tw5/$__plugins_mermaid-tw5_wrapper.js"
      provides: "Clean widget code without debug statements or dead code"
      contains: "mermaidAPI.render(divNode.id, scriptBody, _insertSVG)"
    - path: "mermaid-tw5/plugins/mermaid-tw5/$__plugins_mermaid-tw5_widget-tools.js"
      provides: "Helper library without debug error logging"
      contains: "exports.rocklib = Rocklib"
  key_links: []
---

<objective>
Remove all debug `console.log` statements and commented-out dead code from the plugin source files.

Purpose: Eliminate production noise and technical debt left from development/debugging.
Output: Clean wrapper.js and widget-tools.js with no debug artifacts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@mermaid-tw5/plugins/mermaid-tw5/$__plugins_mermaid-tw5_wrapper.js
@mermaid-tw5/plugins/mermaid-tw5/$__plugins_mermaid-tw5_widget-tools.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove console.log statements from wrapper.js</name>
  <files>mermaid-tw5/plugins/mermaid-tw5/$__plugins_mermaid-tw5_wrapper.js</files>
  <read_first>mermaid-tw5/plugins/mermaid-tw5/$__plugins_mermaid-tw5_wrapper.js</read_first>
  <action>Remove all five `console.log` calls from wrapper.js:
1. Line ~49: `console.log("calling bindFunctions");`
2. Line ~51: `console.log("done calling bindFunctions");`
3. Line ~73: `console.log("Zoom enabled: " + isZoomEnabled);`
4. Line ~75: `console.log("Executing svg.each...");`
5. Line ~77: `console.log("id=" + id);`

Remove the lines entirely (do not leave empty commented lines). Keep the surrounding logic intact — the `if (bindFunctions)` block should remain, just without the log calls. The click handler should remain functionally identical.
</action>
  <verify>grep -n "console.log" mermaid-tw5/plugins/mermaid-tw5/$__plugins_mermaid-tw5_wrapper.js || echo "No console.log found — OK"</verify>
  <acceptance_criteria>
    - grep "console.log" in wrapper.js returns zero matches
    - wrapper.js still contains "if (bindFunctions)" block
    - wrapper.js still contains "divNode.addEventListener('click'" block
    - wrapper.js still contains "mermaidAPI.render(divNode.id, scriptBody, _insertSVG)"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Remove commented-out dead code from wrapper.js</name>
  <files>mermaid-tw5/plugins/mermaid-tw5/$__plugins_mermaid-tw5_wrapper.js</files>
  <read_first>mermaid-tw5/plugins/mermaid-tw5/$__plugins_mermaid-tw5_wrapper.js</read_first>
  <action>Remove the commented-out Node.js/browser detection block (lines ~24-29):
```
// Changes to run on TiddlyWiki for Node.js - 2022-12-28
// if($tw.browser && !window.mermaidAPI) {
//     window.rocklib = new Rocklib();
//     window.mermaidAPI = require("$:/plugins/orange/mermaid-tw5/mermaid.min.js")
//         .mermaidAPI;
// }
```

Also remove the commented-out line ~93:
```
// window.mermaidAPI.render(divNode.id, scriptBody, _insertSVG);
```

Remove all commented lines entirely. Leave a clean gap or adjust spacing to match surrounding code style.
</action>
  <verify>grep -n "window.mermaidAPI\|window.rocklib\|Changes to run on TiddlyWiki for Node.js" mermaid-tw5/plugins/mermaid-tw5/$__plugins_mermaid-tw5_wrapper.js || echo "No dead code found — OK"</verify>
  <acceptance_criteria>
    - grep "window.mermaidAPI" in wrapper.js returns zero matches
    - grep "window.rocklib" in wrapper.js returns zero matches
    - grep "Changes to run on TiddlyWiki for Node.js" in wrapper.js returns zero matches
    - wrapper.js still contains "mermaidAPI.render(divNode.id, scriptBody, _insertSVG)" (the active call)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Remove console.error from widget-tools.js</name>
  <files>mermaid-tw5/plugins/mermaid-tw5/$__plugins_mermaid-tw5_widget-tools.js</files>
  <read_first>mermaid-tw5/plugins/mermaid-tw5/$__plugins_mermaid-tw5_widget-tools.js</read_first>
  <action>In widget-tools.js, find the `console.error(ex)` inside the `getOptions` method catch block (~line 120). Replace it with a silent catch (remove the console.error call but keep the catch block to prevent exceptions from propagating).

The catch should become empty or contain a comment like `// silently ignore parse errors` to document intent.
</action>
  <verify>grep -n "console.error" mermaid-tw5/plugins/mermaid-tw5/$__plugins_mermaid-tw5_widget-tools.js || echo "No console.error found — OK"</verify>
  <acceptance_criteria>
    - grep "console.error" in widget-tools.js returns zero matches
    - widget-tools.js still contains "catch (ex)" in getOptions method
    - getOptions method still ends with "return options;"
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `grep -r "console.log" mermaid-tw5/plugins/mermaid-tw5/*.js` returns no matches
- [ ] `grep -r "console.error" mermaid-tw5/plugins/mermaid-tw5/*.js` returns no matches
- [ ] `grep -r "window.mermaidAPI\|window.rocklib" mermaid-tw5/plugins/mermaid-tw5/*.js` returns no matches
- [ ] All existing examples still render correctly (visual check of demo tiddlers)
</verification>

<success_criteria>
- All console.log and console.error statements removed
- All commented-out dead code removed
- No functional behavior changed
- No new errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/01-cleanup-polish/01-01-SUMMARY.md`
</output>
