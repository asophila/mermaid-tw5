---
phase: 01-cleanup-polish
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - mermaid-tw5/plugins/mermaid-tw5/$__plugins_mermaid-tw5_wrapper.js
  - mermaid-tw5/plugins/mermaid-tw5/$__plugins_mermaid-tw5_widget-tools.js
  - mermaid-tw5/plugins/mermaid-tw5/$__plugins_mermaid-tw5_typed-parser.js
autonomous: true
requirements:
  - STYLE-01

must_haves:
  truths:
    - "All three plugin source files use consistent variable declaration style"
    - "Indentation is uniform (4 spaces) across all plugin source files"
    - "String quotes are consistent (single quotes) across all plugin source files"
  artifacts:
    - path: "mermaid-tw5/plugins/mermaid-tw5/$__plugins_mermaid-tw5_wrapper.js"
      provides: "Widget with consistent JS style"
      contains: "exports.mermaid = MermaidWidget"
    - path: "mermaid-tw5/plugins/mermaid-tw5/$__plugins_mermaid-tw5_widget-tools.js"
      provides: "Helper library with consistent JS style"
      contains: "exports.rocklib = Rocklib"
    - path: "mermaid-tw5/plugins/mermaid-tw5/$__plugins_mermaid-tw5_typed-parser.js"
      provides: "Parser with consistent JS style"
      contains: "exports[\"text/vnd.tiddlywiki.mermaid\"] = MermaidParser"
  key_links: []
---

<objective>
Establish and apply a consistent JavaScript coding style across all plugin source files.

Purpose: Improve readability and maintainability by eliminating mixed conventions (var/let, indentation, quotes).
Output: All three plugin JS files follow a unified style guide.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@mermaid-tw5/plugins/mermaid-tw5/$__plugins_mermaid-tw5_wrapper.js
@mermaid-tw5/plugins/mermaid-tw5/$__plugins_mermaid-tw5_widget-tools.js
@mermaid-tw5/plugins/mermaid-tw5/$__plugins_mermaid-tw5_typed-parser.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Standardize wrapper.js coding style</name>
  <files>mermaid-tw5/plugins/mermaid-tw5/$__plugins_mermaid-tw5_wrapper.js</files>
  <read_first>mermaid-tw5/plugins/mermaid-tw5/$__plugins_mermaid-tw5_wrapper.js</read_first>
  <action>Apply consistent style to wrapper.js using these rules:

1. **Variable declarations:** Use `var` consistently (TiddlyWiki 5 convention; do NOT mix `let` and `var`). Replace all `let` with `var`.
   - Line ~31: `let MermaidWidget = function(...)` → `var MermaidWidget = function(...)`
   - Line ~55: `let options = {` → `var options = {`
   - Line ~69: `let zoomEventListenersApplied = false;` → `var zoomEventListenersApplied = false;`
   - Line ~70: `let isZoomEnabled = false;` → `var isZoomEnabled = false;`
   - Line ~76: `var id = Date.now().toString(36);` (already var, keep)
   - Line ~82: `var zoom = d3.zoom()...` (already var, keep)

2. **Indentation:** Ensure 4-space indentation throughout. Check that the click handler and _insertSVG function are consistently indented.

3. **String quotes:** Use single quotes for all string literals (except the existing comment block which uses `/* ... */` and is fine).
   - `"mermaid"` → `'mermaid'`
   - `"text"` → `'text'`
   - `"Zoom enabled: "` → `'Zoom enabled: '` (but this was removed in Plan 01, may not exist)
   - `"id="` → `'id='` (but this was removed in Plan 01, may not exist)
   - `"<g>"` and `"</g>"` → `'<g>'` and `'</g>'`
   - `"g"` → `'g'`

4. **Ternary style:** Line ~88: `isZoomEnabled?isZoomEnabled=false:isZoomEnabled=true;` → add spaces around `?` and `:`: `isZoomEnabled ? isZoomEnabled = false : isZoomEnabled = true;`

5. **Spacing:** Ensure consistent spacing around operators (`=`, `+`, `!`, etc.) and after commas.

Do NOT change any logic, only style.
</action>
  <verify>grep -n "let " mermaid-tw5/plugins/mermaid-tw5/$__plugins_mermaid-tw5_wrapper.js || echo "No let found — OK"</verify>
  <acceptance_criteria>
    - grep "^    let " or "    let " in wrapper.js returns zero matches
    - wrapper.js uses 4-space indentation (not tabs, not 2 spaces)
    - wrapper.js contains no double-quoted strings inside code (comments and JSDoc are fine)
    - wrapper.js still exports "exports.mermaid = MermaidWidget"
    - wrapper.js still contains "mermaidAPI.render(divNode.id, scriptBody, _insertSVG)"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Standardize widget-tools.js coding style</name>
  <files>mermaid-tw5/plugins/mermaid-tw5/$__plugins_mermaid-tw5_widget-tools.js</files>
  <read_first>mermaid-tw5/plugins/mermaid-tw5/$__plugins_mermaid-tw5_widget-tools.js</read_first>
  <action>Apply consistent style to widget-tools.js:

1. **Decimal literal:** Line ~13: `this.hue = .2;` → `this.hue = 0.2;` (always include leading zero)

2. **Semicolons:** Line ~12: `this.uniqueID = 1;` and line ~13: `this.hue = .2;` — the file mostly uses semicolons. Check that `Rocklib.prototype.getScriptBody` and other method definitions have consistent semicolon usage. Do NOT add semicolons after function declarations (standard JS), but DO ensure semicolons after statements.

3. **Indentation:** Ensure 4-space indentation. Check the switch statements inside `getScriptBody` — they should be consistently indented.

4. **String quotes:** Use single quotes consistently.
   - `"div"` → `'div'`
   - `"canvas"` → `'canvas'`
   - `"height"` → `'height'`
   - `"width"` → `'width'`
   - `"style"` → `'style'`
   - `"id"` → `'id'`

5. **Spacing around operators:** Ensure spaces around `=`, `+`, `===`, `!==`, `>`, `<`, etc.
   - Example: `style += " height:"` should be `style += ' height:'`
   - Example: `canvas.setAttribute("id", tag + "_" + this.uniqueID);` → `canvas.setAttribute('id', tag + '_' + this.uniqueID);`

6. **Consistent `var` usage:** The file already uses `var` predominantly — keep it. Ensure no `let` or `const` sneaked in.

Do NOT change any logic, only style.
</action>
  <verify>grep -n "let \|const " mermaid-tw5/plugins/mermaid-tw5/$__plugins_mermaid-tw5_widget-tools.js || echo "No let/const found — OK"</verify>
  <acceptance_criteria>
    - grep "let " or "const " in widget-tools.js returns zero matches
    - widget-tools.js uses 4-space indentation consistently
    - widget-tools.js contains no double-quoted strings in executable code (comments are fine)
    - widget-tools.js contains "this.hue = 0.2;" (with leading zero)
    - widget-tools.js still exports "exports.rocklib = Rocklib"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Standardize typed-parser.js coding style</name>
  <files>mermaid-tw5/plugins/mermaid-tw5/$__plugins_mermaid-tw5_typed-parser.js</files>
  <read_first>mermaid-tw5/plugins/mermaid-tw5/$__plugins_mermaid-tw5_typed-parser.js</read_first>
  <action>Apply consistent style to typed-parser.js:

1. **Variable declarations:** The file already uses `let`. Since we decided to standardize on `var` for TiddlyWiki 5 compatibility (matching the TW5 codebase convention), replace `let` with `var`:
   - `let MermaidParser = function(...)` → `var MermaidParser = function(...)`
   - `let element = {` → `var element = {`

2. **String quotes:** Use single quotes:
   - `"mermaid"` → `'mermaid'`
   - `"$mermaid"` → `'$mermaid'`
   - `"text/vnd.tiddlywiki.mermaid"` → keep as-is (this is a MIME type used as object key, double quotes are required for the key syntax `exports["..."]`, but the string literal inside can use single quotes if we change the bracket notation — however, to minimize risk, leave `exports["text/vnd.tiddlywiki.mermaid"]` as-is or convert to `exports['text/vnd.tiddlywiki.mermaid']`)

3. **Indentation:** Ensure 4-space indentation.

Do NOT change any logic, only style.
</action>
  <verify>grep -n "let " mermaid-tw5/plugins/mermaid-tw5/$__plugins_mermaid-tw5_typed-parser.js || echo "No let found — OK"</verify>
  <acceptance_criteria>
    - grep "let " in typed-parser.js returns zero matches
    - typed-parser.js uses 4-space indentation
    - typed-parser.js contains "var MermaidParser = function(type, text, options)"
    - typed-parser.js contains "var element = {"
    - typed-parser.js still exports the parser: `exports["text/vnd.tiddlywiki.mermaid"] = MermaidParser` or `exports['text/vnd.tiddlywiki.mermaid'] = MermaidParser`
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] All three JS files use `var` exclusively (no `let` or `const`)
- [ ] All three JS files use 4-space indentation consistently
- [ ] All three JS files use single quotes for string literals (except where double quotes are syntactically required)
- [ ] `grep -r "let \|const " mermaid-tw5/plugins/mermaid-tw5/*.js` returns no matches (excluding minified libraries)
- [ ] Examples continue to render correctly after style changes
</verification>

<success_criteria>
- All tasks completed
- Consistent coding style applied across wrapper.js, widget-tools.js, and typed-parser.js
- No functional changes introduced
- No new errors or warnings
</success_criteria>

<output>
After completion, create `.planning/phases/01-cleanup-polish/01-02-SUMMARY.md`
</output>
